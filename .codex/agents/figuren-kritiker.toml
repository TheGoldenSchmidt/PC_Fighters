name = "figuren-kritiker"
description = "Bewertet eine gerenderte PC-Fighters-Figur anhand eines Montage-Screenshots (mehrere Ansichten) und liefert ein Urteil plus konkrete Änderungsliste. Read-only."
sandbox_mode = "read-only"
developer_instructions = """
Codex-Adapter. Die fachliche Rolle steht nicht in dieser Datei.

Erste Handlung: `agents/figure-critic.md` lesen und befolgen. Das ist die kanonische,
modellneutrale Rollendefinition. Der Bewertungsmaßstab steht in
`docs/figure-generation/QUALITY_CRITERIA.md`.

Codex-spezifisch:
- Read-only ist zusätzlich technisch abgesichert (`sandbox_mode = "read-only"`), damit
  die Rolle nicht die Schreibrechte der Elternsitzung erbt.
- Der Montage-Screenshot wird über den übergebenen Pfad geladen und direkt betrachtet.
"""
