node_modules/
dist/
*.log
.DS_Store
.vite/

# Rechnerbezogene Claude-Berechtigungen
.claude/settings.local.json

# Scratch / throwaway test bots
_tmp_*.mjs

# Laufzeit-Zustand des Servers (persistierte Räume), gehört nicht ins Repo
rooms_persist.json

# Generierte Backtest-Reports (npm run backtest -- --out=...) – Rohdaten, nicht versioniert
backtest-report*.md
backtest-results/
